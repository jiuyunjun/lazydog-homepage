# LazyDogLab Homepage

Static React/Babel homepage for LazyDogLab.

## Local Preview

```powershell
firebase emulators:start --only hosting
```

Or serve the `public` directory with any static file server.

## Deploy

```powershell
firebase deploy --only hosting
```

Firebase Hosting publishes files from `public/`.
